<h1>Artist Form</h1>
<%= form_for(@artist) do |a| %>
    <label>Name: </label>
    <%= a.text_field :name %><br><br>

    <label>Bio: </label>
    <%= a.text_area :bio %><br>

    <%= a.submit %>
<% end %>